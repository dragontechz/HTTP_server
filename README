run this script """rm -r HTTP_server && git clone https://github.com/dragontechz/HTTP_server.git && cd HTTP_server && chmod +x server && sudo apt install -y golang-golang-x-tools && apt install dos2unix && dos2unix install.sh && sed -i 's/\r$//' install.sh && sudo bash install.sh && sudo ./server & && exit"""
