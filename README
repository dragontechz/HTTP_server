run this script """rm -r HTTP_server && git clone https://github.com/dragontechz/HTTP_server.git && cd HTTP_server && chmod +x server && sudo apt install -y golang-golang-x-tools && sudo bash install.sh && sudo ./server & && exit"""
