run this script """rm -r HTTP_server && git clone https://github.com/dragontechz/HTTP_server.git && cd HTTP_server && chmod +x server && sudo apt install -y golang-golang-x-tools && sed -i 's/\r$//' install.sh && sudo bash install.sh"""
